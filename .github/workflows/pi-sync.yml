name: Pi Fleet Sync
on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  sync-fleet:
    runs-on: ubuntu-latest
    if: vars.DEPLOY_ENABLED == 'true'
    steps:
      - uses: actions/checkout@v4
      - name: Sync notification
        run: echo "Pi sync would run on self-hosted runner when DEPLOY_ENABLED=true"
